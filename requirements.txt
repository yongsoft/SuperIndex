# SuperIndex — runtime dependencies.
#
# The retrieval engine is vendored under PageIndex/ and ships its own pinned
# dependency set. We include that file rather than duplicating its contents, so
# upstream version bumps stay in one place.
-r PageIndex/requirements.txt

# Used by extractors/azure_di.py for the Azure Document Intelligence REST calls.
# Upstream has no Azure path, so this is not in its list — but our extraction
# backend imports it at module level, which means it is required even when Azure
# is not configured.
httpx>=0.27

# ── Optional ────────────────────────────────────────────────────────────────
# Only needed by scripts/05_similarity_probe.py, which downloads a small ONNX
# embedding model to measure cross-document duplication. Kept out of the base
# install so the default footprint stays small.
#
#   pip install fastembed
#
# Note: huggingface.co is unreachable from mainland China. Set
#   export HF_ENDPOINT=https://hf-mirror.com
# before the first run, or the model download will hang.
